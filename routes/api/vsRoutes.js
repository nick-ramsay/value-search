const router = require("express").Router();
const vsControllers = require("../../controllers/vsControllers");

/*
router
  .route("/create-message")
  .post(vsControllers.createMessage);

router
  .route("/find-all-messages")
  .post(vsControllers.findAllMessages);

router
  .route("/delete-one-message")
  .post(vsControllers.deleteOneMessage);

*/

module.exports = router;
